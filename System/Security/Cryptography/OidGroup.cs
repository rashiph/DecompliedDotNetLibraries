namespace System.Security.Cryptography
{
    using System;

    internal enum OidGroup
    {
        AllGroups,
        HashAlgorithm,
        EncryptionAlgorithm,
        PublicKeyAlgorithm,
        SignatureAlgorithm,
        Attribute,
        ExtensionOrAttribute,
        EnhancedKeyUsage,
        Policy,
        Template
    }
}

