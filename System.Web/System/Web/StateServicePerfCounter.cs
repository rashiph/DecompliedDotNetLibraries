namespace System.Web
{
    using System;

    internal enum StateServicePerfCounter
    {
        STATE_SERVICE_SESSIONS_ABANDONED = 0x5b,
        STATE_SERVICE_SESSIONS_ACTIVE = 90,
        STATE_SERVICE_SESSIONS_TIMED_OUT = 0x5c,
        STATE_SERVICE_SESSIONS_TOTAL = 0x5d
    }
}

