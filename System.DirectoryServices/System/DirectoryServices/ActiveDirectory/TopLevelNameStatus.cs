namespace System.DirectoryServices.ActiveDirectory
{
    using System;

    public enum TopLevelNameStatus
    {
        AdminDisabled = 2,
        ConflictDisabled = 4,
        Enabled = 0,
        NewlyCreated = 1
    }
}

