namespace System.Management
{
    using System;

    internal enum tag_WBEM_SHUTDOWN_FLAGS
    {
        WBEM_SHUTDOWN_OS = 3,
        WBEM_SHUTDOWN_UNLOAD_COMPONENT = 1,
        WBEM_SHUTDOWN_WMI = 2
    }
}

